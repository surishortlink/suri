spec:
  name: suri
  static_sites:
    - name: suri
      git:
        repo_clone_url: https://github.com/jstayton/suri.git
        branch: master
      envs:
        - key: SURI_JS
          value: '0'
      build_command: npm run build
      output_dir: _site
